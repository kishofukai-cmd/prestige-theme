<style>
/* ============================================================
   KAPOK KNOT MUSUBI -en- | Rich LP Stylesheet
   Last CI deploy test: staging branch auto-deploy
   モバイルファースト設計 / 1スクロール1メッセージ
   ============================================================ */

:root {
  /* Colors - 温かみのあるプレミアムパレット（売れるLP配色） */
  --color-bg: #faf8f5;
  --color-bg-warm: #f2ede5;
  --color-bg-cream: #f7f3ec;
  --color-bg-dark: #3a3530;
  --color-bg-dark-soft: #4a4540;
  --color-text: #2c2825;
  --color-text-sub: #6b635a;
  --color-text-light: #faf8f5;
  --color-accent: #a88a64;
  --color-accent-light: #d4c2ab;
  --color-gold: #c29d66;
  --color-border: #e0dcd1;
  --color-tag: #ece7da;

  /* Typography */
  --font-serif: 'Noto Serif JP', 'YuMincho', serif;
  --font-sans: 'Noto Sans JP', 'Hiragino Sans', sans-serif;
  --font-display: 'Cormorant Garamond', var(--font-serif);

  /* Spacing - ワンノバ風の極端な余白とエッジなし */
  --section-gap: clamp(80px, 15vw, 160px);
  --section-gap-tight: clamp(40px, 8vw, 80px);
  --container-max: 960px;
  --container-narrow: 680px;
  --side-pad: clamp(16px, 5vw, 40px);

  /* Transition */
  --ease: cubic-bezier(0.16, 1, 0.3, 1);
  --dur: 0.7s;
}

/* === Reset === */
*,
*::before,
*::after {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
  -webkit-font-smoothing: antialiased;
  font-size: 16px;
  overflow-x: hidden;
}

body {
  font-family: var(--font-sans);
  color: var(--color-text);
  background: var(--color-bg);
  line-height: 1.85;
  overflow-x: hidden;
  max-width: 100vw;
  width: 100%;
}

img {
  max-width: 100%;
  height: auto;
  display: block;
}

a {
  color: inherit;
  text-decoration: none;
}

button {
  font-family: inherit;
}

/* === Scrollbar === */
::-webkit-scrollbar {
  width: 4px;
}

::-webkit-scrollbar-thumb {
  background: var(--color-accent-light);
  border-radius: 2px;
}

/* === Container === */
.container {
  max-width: var(--container-max);
  margin: 0 auto;
  padding: 0 var(--side-pad);
}

.container--narrow {
  max-width: var(--container-narrow);
  margin: 0 auto;
  padding: 0 var(--side-pad);
}

/* === Reveal === */
[data-reveal] {
  opacity: 0;
  transform: translateY(32px);
  transition: opacity var(--dur) var(--ease), transform var(--dur) var(--ease);
}

[data-reveal].revealed {
  opacity: 1;
  transform: translateY(0);
}

/* === Section Title === */
.section-title {
  font-family: var(--font-serif);
  font-size: clamp(1.75rem, 6vw, 2.5rem);
  font-weight: 600;
  line-height: 1.4;
  text-align: center;
  letter-spacing: 0.05em;
  margin-bottom: clamp(48px, 8vw, 72px);
}

.section-title--light {
  color: var(--color-text-light);
}

/* ============================================================
   HEADER
   ============================================================ */
.header {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 1000;
  background: rgba(250, 249, 246, 0.92);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border-bottom: 1px solid transparent;
  transition: border-color 0.4s ease;
  width: 100%;
  max-width: 100vw;
}

.header.scrolled {
  border-color: var(--color-border);
}

.header__inner {
  max-width: var(--container-max);
  margin: 0 auto;
  padding: 0 var(--side-pad);
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 56px;
}

.header__logo-img {
  height: 20px;
  width: auto;
}

.header__nav {
  display: flex;
  gap: 24px;
}

.header__nav a {
  font-size: 0.75rem;
  font-weight: 400;
  letter-spacing: 0.08em;
  color: var(--color-text-sub);
  transition: color 0.3s ease;
}

.header__nav a:hover {
  color: var(--color-text);
}

.header__hamburger {
  display: none;
  flex-direction: column;
  gap: 5px;
  background: none;
  border: none;
  cursor: pointer;
  padding: 8px;
  z-index: 1001;
}

.header__hamburger span {
  width: 22px;
  height: 1.5px;
  background: var(--color-text);
  transition: all 0.3s ease;
  display: block;
}

.header__hamburger.active span:nth-child(1) {
  transform: rotate(45deg) translate(4px, 4px);
}

.header__hamburger.active span:nth-child(2) {
  opacity: 0;
}

.header__hamburger.active span:nth-child(3) {
  transform: rotate(-45deg) translate(4px, -4px);
}

@media (max-width: 768px) {
  .header__nav {
    display: none;
  }

  .header__hamburger {
    display: flex;
  }
}

/* Mobile nav overlay */
.mobile-nav {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 999;
  background: rgba(250, 249, 246, 0.98);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 32px;
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.4s ease;
}

.mobile-nav.open {
  opacity: 1;
  pointer-events: all;
}

.mobile-nav__link {
  font-family: var(--font-serif);
  font-size: 1.25rem;
  letter-spacing: 0.1em;
  color: var(--color-text);
}

/* ============================================================
   HERO - 全画面化
   ============================================================ */
.hero {
  position: relative;
  min-height: 100svh;
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
  background: var(--color-bg-dark);
  color: var(--color-text-light);
}

.hero__img-wrapper {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 1;
}

.hero__img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center 30%;
  opacity: 0.85;
  /* テキストの視認性担保 */
}

/* 画像の下部をグラデーションで暗くしてテキストを浮き立たせる */
.hero__img-wrapper::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 0;
  width: 100%;
  height: 60%;
  background: linear-gradient(to top, rgba(18, 18, 18, 0.9), transparent);
}

.hero__content {
  position: relative;
  z-index: 2;
  padding: 0 var(--side-pad) clamp(40px, 10vw, 80px);
  text-align: center;
}

.hero__brand {
  font-family: var(--font-display);
  font-size: 0.75rem;
  letter-spacing: 0.4em;
  text-indent: 0.4em;
  /* 中央ズレ補正 */
  color: var(--color-gold);
  margin-bottom: 24px;
}

.hero__title {
  font-family: var(--font-serif);
  font-size: clamp(2rem, 8vw, 3.5rem);
  font-weight: 500;
  line-height: 1.4;
  letter-spacing: 0.05em;
  margin-bottom: 24px;
  text-shadow: 0 4px 12px rgba(0, 0, 0, 0.4);
}

.hero__subtitle {
  font-size: clamp(0.9rem, 3.5vw, 1.1rem);
  color: rgba(247, 245, 240, 0.9);
  line-height: 1.8;
  margin-bottom: 48px;
  letter-spacing: 0.04em;
}

/* Social Proof bar - Hero内はダークモードに */
.hero__proof {
  display: flex;
  justify-content: center;
  align-items: center;
  background: rgba(255, 255, 255, 0.08);
  backdrop-filter: blur(8px);
  border: 1px solid rgba(255, 255, 255, 0.15);
  border-radius: 100px;
  /* カプセル型 */
  padding: 20px 0;
  max-width: 480px;
  margin: 0 auto;
}

.hero__proof-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  flex: 1;
  padding: 0 12px;
}

.hero__proof-number {
  font-family: var(--font-display);
  font-size: clamp(1.25rem, 4vw, 1.5rem);
  font-weight: 500;
  letter-spacing: 0.03em;
  color: #fff;
}

.hero__proof-label {
  font-size: 0.7rem;
  color: rgba(255, 255, 255, 0.7);
  letter-spacing: 0.05em;
  margin-top: 4px;
}

.hero__proof-divider {
  width: 1px;
  height: 40px;
  background: rgba(255, 255, 255, 0.2);
}

/* ヒーローCTA */
.hero__cta-area {
  margin-top: 32px;
  text-align: center;
}

.hero__cta-btn {
  display: inline-block;
  padding: 16px 48px;
  font-family: var(--font-serif);
  font-size: 1rem;
  font-weight: 500;
  letter-spacing: 0.1em;
  color: #fff;
  background: linear-gradient(135deg, var(--color-gold), #c9a56c);
  border-radius: 100px;
  text-decoration: none;
  box-shadow: 0 4px 20px rgba(183, 155, 105, 0.4);
  transition: all 0.3s ease;
}

.hero__cta-btn:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 28px rgba(183, 155, 105, 0.5);
}

.hero__price {
  margin-top: 12px;
  font-family: var(--font-display);
  font-size: 1.1rem;
  font-weight: 500;
  color: rgba(255, 255, 255, 0.9);
  letter-spacing: 0.05em;
}

.hero__price-tax {
  font-size: 0.75rem;
  color: rgba(255, 255, 255, 0.6);
}

/* ============================================================
   GUARANTEE (安心保証バナー)
   ============================================================ */
.guarantee {
  background: var(--color-bg);
  border-bottom: 1px solid var(--color-border);
  padding: 20px 0;
}

.guarantee__list {
  display: flex;
  justify-content: space-around;
  align-items: center;
  flex-wrap: wrap;
  gap: 16px;
}

.guarantee__item {
  display: flex;
  align-items: center;
  gap: 10px;
}

.guarantee__icon {
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--color-accent);
}

.guarantee__icon svg {
  width: 20px;
  height: 20px;
}

.guarantee__text {
  font-size: 0.85rem;
  font-weight: 600;
  letter-spacing: 0.05em;
  color: var(--color-text);
  white-space: nowrap;
}

@media (min-width: 768px) {
  .guarantee {
    padding: 28px 0;
  }
  .guarantee__list {
    flex-wrap: nowrap;
    justify-content: center;
    gap: 64px;
  }
  .guarantee__icon svg {
    width: 24px;
    height: 24px;
  }
  .guarantee__text {
    font-size: 1rem;
  }
}

/* ============================================================
   VIDEO LOOP (パララックス)
   ============================================================ */
.video-loop {
  position: relative;
  height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  background: var(--color-bg-warm);
}

.video-loop__bg {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
}

.video-loop__bg img,
.video-loop__video {
  width: 100%;
  height: 100%;
  object-fit: cover;
  opacity: 0.6;
}

.video-loop__overlay {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: linear-gradient(to bottom, rgba(18, 18, 18, 0.3), rgba(18, 18, 18, 0.8));
}

.video-loop__content {
  position: relative;
  z-index: 2;
  text-align: center;
  color: #fff;
  padding: 0 var(--side-pad);
}

.video-loop__content h2 {
  font-family: var(--font-serif);
  font-size: clamp(1.75rem, 8vw, 3.5rem);
  font-weight: 400;
  line-height: 1.6;
  letter-spacing: 0.1em;
}

/* ============================================================
   WHY - 強烈なスクロール演出 (Edge to Edge)
   ============================================================ */
.why {
  padding: var(--section-gap) 0;
  background: var(--color-bg);
}

.why .section-title {
  max-width: var(--container-max);
  margin: 0 auto clamp(48px, 10vw, 80px);
  padding: 0 var(--side-pad);
}

.why__cards {
  display: flex;
  flex-direction: column;
  gap: var(--section-gap);
  /* セクションごとの間隔を大きく */
}

/* カードの枠を外し、画面端まで画像をデカデカと配置 */
.why__card {
  display: flex;
  flex-direction: column;
  background: transparent;
  border: none;
  border-radius: 0;
  overflow: visible;
}

.why__card-img {
  width: 100%;
  aspect-ratio: 4/5;
  /* 縦長で迫力を */
  overflow: hidden;
  margin-bottom: 32px;
}

.why__card-img img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  /* 画像端まで広げる */
}

.why__card-content {
  padding: 0 var(--side-pad);
  max-width: var(--container-narrow);
  margin: 0 auto;
}

.why__card-num {
  font-family: var(--font-display);
  font-size: clamp(3rem, 10vw, 5rem);
  /* 巨大な数字 */
  line-height: 1;
  color: var(--color-accent);
  opacity: 0.3;
  /* 透かして背景化 */
  display: block;
  margin-bottom: 0px;
  margin-left: -4px;
}

.why__card-title {
  font-family: var(--font-serif);
  font-size: clamp(1.75rem, 6vw, 2.25rem);
  font-weight: 500;
  margin-top: -24px;
  /* 数字と少し重ねる */
  position: relative;
  z-index: 2;
  margin-bottom: 24px;
  line-height: 1.4;
}

.why__card-desc {
  font-size: 1rem;
  color: var(--color-text-sub);
  line-height: 2;
}

@media (min-width: 768px) {
  .why__card {
    flex-direction: row;
    align-items: center;
    gap: 64px;
    max-width: var(--container-max);
    margin: 0 auto;
    padding: 0 var(--side-pad);
  }

  .why__card:nth-child(even) {
    flex-direction: row-reverse;
  }

  .why__card-img {
    flex: 1;
    aspect-ratio: 1/1;
    border-radius: 16px;
    margin-bottom: 0;
  }

  .why__card-content {
    flex: 1;
    padding: 0;
  }
}

/* ============================================================
   EMPATHY - 没入感強化 (The Problem)
   ============================================================ */
.empathy {
  padding: var(--section-gap) 0;
  background: var(--color-bg-warm);
  /* 温かいベージュで共感を演出 */
  color: var(--color-text);
}

.empathy__title {
  font-family: var(--font-serif);
  font-size: clamp(1.75rem, 6vw, 2.5rem);
  font-weight: 500;
  text-align: center;
  margin-bottom: 48px;
  letter-spacing: 0.02em;
}

.empathy__list {
  display: flex;
  flex-direction: column;
  gap: 16px;
  max-width: 480px;
  margin: 0 auto 40px;
}

.empathy__item {
  display: flex;
  align-items: center;
  gap: 20px;
  background: rgba(194, 157, 102, 0.08);
  border: 1px solid rgba(194, 157, 102, 0.2);
  border-radius: 16px;
  /* 丸み強化 */
  padding: 24px;
}

.empathy__icon {
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--color-gold);
}

.empathy__item p {
  font-size: 1.05rem;
  font-weight: 400;
  color: var(--color-text);
}

.empathy__arrow {
  text-align: center;
  font-size: 2rem;
  color: var(--color-gold);
  margin-bottom: 48px;
  line-height: 1;
  animation: bounce 2s infinite;
}

@keyframes bounce {

  0%,
  20%,
  50%,
  80%,
  100% {
    transform: translateY(0);
  }

  40% {
    transform: translateY(-10px);
  }

  60% {
    transform: translateY(-5px);
  }
}

.empathy__solution {
  text-align: center;
  position: relative;
  /* 画像を両端ギリギリまで広げる */
  margin-left: calc(-1 * var(--side-pad));
  margin-right: calc(-1 * var(--side-pad));
}

.empathy__solution-img {
  width: 100%;
  overflow: hidden;
  margin-bottom: 0;
}

@media (min-width: 768px) {
  .empathy__solution {
    margin-left: auto;
    margin-right: auto;
    max-width: 900px;
  }

  .empathy__solution-img {
    border-radius: 16px;
    aspect-ratio: 16/9;
  }
}

.empathy__solution-img img {
  width: 100%;
  height: auto;
  display: block;
}

@media (min-width: 768px) {
  .empathy__solution-img img {
    height: 100%;
    object-fit: cover;
  }
}

/* ワンノバ風：画像上にテキストを大きく重ねる（読みやすいバンド付き） */
.empathy__solution-text {
  position: absolute;
  bottom: 0;
  left: 0;
  width: 100%;
  padding: 40px 20px 36px;
  background: rgba(250, 248, 245, 0.88);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  font-family: var(--font-serif);
  font-size: clamp(1.4rem, 5vw, 2rem);
  font-weight: 500;
  line-height: 1.6;
  letter-spacing: 0.05em;
  color: var(--color-text);
  text-align: center;
}

.empathy__solution-text strong {
  color: var(--color-accent);
  font-weight: 700;
  display: block;
  font-size: 1.2em;
  margin-top: 4px;
}

/* ============================================================
   MATERIAL - カポック素材ストーリー
   ============================================================ */
.material {
  padding: var(--section-gap) 0;
  background: var(--color-bg);
}

.material__inner {
  display: flex;
  flex-direction: column;
  gap: 40px;
}

.material__img {
  width: 100%;
  border-radius: 16px;
  overflow: hidden;
  background: var(--color-bg-warm);
}

.material__img img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

.material__img-placeholder {
  padding: 80px 20px;
  text-align: center;
  color: var(--color-text-sub);
  font-size: 0.9rem;
}

.material__content {
  display: flex;
  flex-direction: column;
  justify-content: center;
}

.material__label {
  font-family: var(--font-display);
  font-size: 0.85rem;
  letter-spacing: 0.2em;
  color: var(--color-accent);
  margin-bottom: 12px;
}

.material__title {
  font-family: var(--font-serif);
  font-size: clamp(1.5rem, 6vw, 2.25rem);
  font-weight: 500;
  margin-bottom: 24px;
  line-height: 1.4;
}

.material__desc {
  font-size: 0.95rem;
  color: var(--color-text-sub);
  line-height: 1.9;
  margin-bottom: 32px;
}

.material__desc p {
  margin-bottom: 16px;
}

.material__desc strong {
  font-weight: 600;
  color: var(--color-text);
  background: linear-gradient(transparent 60%, rgba(194, 157, 102, 0.3) 60%);
}

.material__features {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.material__features li {
  font-size: 0.95rem;
  font-weight: 500;
  color: var(--color-text);
}

@media (min-width: 768px) {
  .material__inner {
    flex-direction: row;
    align-items: center;
    gap: 60px;
  }
  
  .material__img, .material__content {
    flex: 1;
  }
}

/* ============================================================
   STORY - ストーリーテリング
   ============================================================ */
.story {
  padding: var(--section-gap) 0;
  background: var(--color-bg);
  text-align: center;
}

.story__title {
  font-family: var(--font-serif);
  font-size: clamp(1.5rem, 6vw, 2.25rem);
  font-weight: 500;
  margin-bottom: 48px;
  letter-spacing: 0.05em;
}

.story__content {
  max-width: 600px;
  margin: 0 auto;
}

.story__lead {
  font-size: clamp(1.1rem, 4vw, 1.25rem);
  font-weight: 500;
  color: var(--color-accent);
  margin-bottom: 32px;
  line-height: 1.8;
}

.story__content p {
  font-size: 0.9375rem;
  color: var(--color-text-sub);
  line-height: 2;
  margin-bottom: 24px;
}

.story__content strong {
  font-weight: 600;
  color: var(--color-text);
  font-size: 1.05rem;
}

/* ============================================================
   SCIENCE
   ============================================================ */
.science {
  padding: var(--section-gap) 0;
  background: var(--color-bg-dark);
  color: var(--color-text-light);
}

.science__diagram {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0;
  margin-bottom: 40px;
  flex-wrap: wrap;
}

.science__step {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 10px;
  padding: 0 12px;
}

.science__step-circle {
  width: 80px;
  height: 80px;
  border-radius: 50%;
  border: 1px solid rgba(255, 255, 255, 0.15);
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(255, 255, 255, 0.02);
  color: #fff;
}

.science__step-label {
  font-size: 0.8125rem;
  text-align: center;
  line-height: 1.5;
  color: rgba(243, 239, 232, 0.8);
}

.science__step-label small {
  font-family: var(--font-display);
  font-size: 0.65rem;
  color: rgba(243, 239, 232, 0.5);
  letter-spacing: 0.05em;
}

.science__connector {
  width: 40px;
  height: 2px;
  background: linear-gradient(90deg, transparent, var(--color-gold), transparent);
  margin-bottom: 24px;
}

@media (max-width: 480px) {
  .science__diagram {
    flex-direction: column;
    gap: 8px;
  }

  .science__connector {
    width: 2px;
    height: 24px;
    background: linear-gradient(180deg, transparent, var(--color-gold), transparent);
    margin-bottom: 0;
  }
}

.science__img {
  border-radius: 12px;
  overflow: hidden;
  margin-bottom: 32px;
  max-width: 600px;
  margin-left: auto;
  margin-right: auto;
}

.science__text {
  max-width: 560px;
  margin: 0 auto 40px;
  text-align: center;
}

.science__text p {
  font-size: 0.9375rem;
  line-height: 2;
  color: rgba(243, 239, 232, 0.75);
}

.science__text strong {
  color: var(--color-gold);
  font-weight: 500;
}

/* Papers */
.science__papers {
  max-width: 560px;
  margin: 0 auto;
  background: rgba(255, 255, 255, 0.04);
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: 12px;
  padding: 24px;
}

.science__papers-title {
  font-size: 0.8125rem;
  font-weight: 500;
  margin-bottom: 12px;
  color: rgba(243, 239, 232, 0.9);
}

.science__papers-list {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 8px;
  margin-bottom: 12px;
}

.science__papers-list li a {
  font-size: 0.75rem;
  color: var(--color-gold);
  text-decoration: underline;
  text-underline-offset: 3px;
  line-height: 1.6;
  display: block;
  transition: opacity 0.3s ease;
}

.science__papers-list li a:hover {
  opacity: 0.7;
}

.science__papers-note {
  font-size: 0.7rem;
  color: rgba(243, 239, 232, 0.4);
}

/* ============================================================
   SIZES
   ============================================================ */
.sizes {
  padding: var(--section-gap) 0;
  background: var(--color-bg);
  /* 白背景にして前後のDarkと明暗を分ける */
}

/* Tabs: スマホで指が届く超巨大タブに */
.sizes__tabs {
  display: flex;
  gap: 8px;
  max-width: 500px;
  margin: 0 auto 40px;
}

.sizes__tab {
  flex: 1;
  padding: 18px 0;
  font-size: 1.05rem;
  font-weight: 600;
  letter-spacing: 0.03em;
  background: transparent;
  border: 2px solid var(--color-border);
  border-radius: 16px;
  cursor: pointer;
  color: var(--color-text-sub);
  transition: all 0.3s ease;
}

.sizes__tab--active {
  background: var(--color-accent);
  color: #fff;
  border-color: var(--color-accent);
  box-shadow: 0 4px 16px rgba(168, 138, 100, 0.3);
}

/* Panel - カードの枠を外し画像フルサイズ */
.sizes__panel {
  display: none;
  margin-bottom: 24px;
  /* full bleed on mobile */
  margin-left: calc(-1 * var(--side-pad));
  margin-right: calc(-1 * var(--side-pad));
}

.sizes__panel--active {
  display: block;
}

.sizes__panel-img {
  aspect-ratio: 1/1;
  overflow: hidden;
}

@media (min-width: 768px) {
  .sizes__panel {
    margin-left: 0;
    margin-right: 0;
    border-radius: 20px;
    overflow: hidden;
    background: var(--color-card);
  }
}

.sizes__panel-img img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.sizes__panel-content {
  padding: 40px var(--side-pad);
  background: var(--color-card);
}

.sizes__panel-badge {
  display: inline-block;
  font-size: 0.7rem;
  letter-spacing: 0.1em;
  background: var(--color-tag);
  color: var(--color-accent);
  padding: 4px 14px;
  border-radius: 100px;
  margin-bottom: 12px;
}

.sizes__panel-badge--featured {
  background: var(--color-accent);
  color: #fff;
}

.sizes__panel-name {
  font-family: var(--font-display);
  font-size: 1.5rem;
  font-weight: 400;
  letter-spacing: 0.05em;
  margin-bottom: 6px;
}

.sizes__panel-price {
  font-family: var(--font-display);
  font-size: 1.4rem;
  font-weight: 600;
  color: var(--color-accent);
  letter-spacing: 0.03em;
  margin-bottom: 8px;
}

.sizes__panel-price span {
  font-size: 0.8rem;
  font-weight: 400;
  color: var(--color-text-sub);
}

.sizes__panel-dim {
  font-family: var(--font-display);
  font-size: 1rem;
  color: var(--color-text-sub);
  letter-spacing: 0.03em;
  margin-bottom: 16px;
}

.sizes__panel-features {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 6px;
  margin-bottom: 16px;
}

.sizes__panel-features li {
  font-size: 0.875rem;
  color: var(--color-text);
  padding-left: 18px;
  position: relative;
}

.sizes__panel-features li::before {
  content: '✓';
  position: absolute;
  left: 0;
  color: var(--color-accent);
  font-weight: 600;
}

.sizes__panel-note {
  font-size: 0.8125rem;
  color: var(--color-text-sub);
  line-height: 1.8;
}

/* Compare */
.sizes__compare {
  border-radius: 12px;
  overflow: hidden;
  margin-top: 8px;
}

@media (min-width: 640px) {
  .sizes__panel {
    display: none;
  }

  .sizes__panel--active {
    display: grid;
    grid-template-columns: 1fr 1fr;
  }

  .sizes__panel-img {
    aspect-ratio: auto;
    min-height: 360px;
  }
}

/* ============================================================
   COLORS - ダーク反転 & エッジ拡張
   ============================================================ */
.colors {
  padding: var(--section-gap) 0;
  background: var(--color-bg-cream);
  color: var(--color-text);
}

.colors__showcase {
  margin-left: calc(-1 * var(--side-pad));
  margin-right: calc(-1 * var(--side-pad));
  margin-bottom: 16px;
}

@media (min-width: 768px) {
  .colors__showcase {
    margin-left: auto;
    margin-right: auto;
    border-radius: 16px;
    overflow: hidden;
    max-width: 600px;
  }
}

.colors__showcase img {
  width: 100%;
  height: auto;
  transition: opacity 0.4s ease;
}

.colors__name {
  text-align: center;
  font-family: var(--font-display);
  font-size: 0.9rem;
  letter-spacing: 0.15em;
  color: rgba(243, 239, 232, 0.7);
  margin-bottom: 20px;
}

.colors__swatches {
  display: flex;
  justify-content: center;
  gap: 14px;
  margin-bottom: 40px;
}

.colors__swatch {
  width: 36px;
  height: 36px;
  border-radius: 50%;
  border: 2px solid transparent;
  cursor: pointer;
  transition: all 0.3s ease;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
}

.colors__swatch:hover {
  transform: scale(1.15);
}

.colors__swatch--active {
  border-color: var(--color-gold);
  box-shadow: 0 0 0 3px var(--color-bg-dark), 0 0 0 5px var(--color-gold);
}

/* カラー別カルーセル */
.colors__carousel {
  position: relative;
  display: flex;
  align-items: center;
  gap: 8px;
  max-width: 600px;
  margin: 0 auto;
}

.colors__carousel-track {
  display: flex;
  gap: 12px;
  overflow-x: auto;
  scroll-behavior: smooth;
  scroll-snap-type: x mandatory;
  -ms-overflow-style: none;
  scrollbar-width: none;
  padding: 8px 0;
}

.colors__carousel-track::-webkit-scrollbar {
  display: none;
}

.colors__carousel-item {
  flex: 0 0 calc(33.333% - 8px);
  scroll-snap-align: start;
  border-radius: 12px;
  overflow: hidden;
  cursor: pointer;
  border: 2px solid transparent;
  transition: all 0.3s ease;
  background: rgba(255, 255, 255, 0.05);
}

.colors__carousel-item:hover {
  border-color: rgba(255, 255, 255, 0.3);
}

.colors__carousel-item--active {
  border-color: var(--color-gold);
}

.colors__carousel-item img {
  width: 100%;
  aspect-ratio: 1/1;
  object-fit: cover;
  display: block;
}

.colors__carousel-item span {
  display: block;
  text-align: center;
  font-family: var(--font-display);
  font-size: 0.7rem;
  letter-spacing: 0.08em;
  color: rgba(243, 239, 232, 0.8);
  padding: 10px 4px;
}

.colors__carousel-arrow {
  flex-shrink: 0;
  width: 36px;
  height: 36px;
  border-radius: 50%;
  border: 1px solid rgba(255, 255, 255, 0.2);
  background: rgba(255, 255, 255, 0.08);
  color: #fff;
  font-size: 1.4rem;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.3s ease;
}

.colors__carousel-arrow:hover {
  background: rgba(255, 255, 255, 0.15);
  border-color: var(--color-gold);
}

/* ============================================================
   CARE - 日常のケア
   ============================================================ */
.care {
  padding: var(--section-gap) 0;
  background: var(--color-bg);
}

.care__layout {
  display: flex;
  flex-direction: column;
  gap: 40px;
}

.care__img {
  width: 100%;
  aspect-ratio: 4/5;
  border-radius: 16px;
  overflow: hidden;
}

.care__img img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.care__content {
  display: flex;
  flex-direction: column;
  gap: 24px;
}

.care__point {
  background: var(--color-card);
  border: 1px solid var(--color-border);
  border-radius: 16px;
  padding: 32px 24px;
}

.care__icon {
  font-size: 2rem;
  display: block;
  margin-bottom: 16px;
}

.care__point h3 {
  font-family: var(--font-serif);
  font-size: 1.125rem;
  font-weight: 600;
  margin-bottom: 12px;
}

.care__point p {
  font-size: 0.9375rem;
  color: var(--color-text-sub);
  line-height: 1.8;
}

@media (min-width: 768px) {
  .care__layout {
    flex-direction: row;
    align-items: center;
  }

  .care__img {
    flex: 1;
    margin-bottom: 0;
    aspect-ratio: 3/4;
  }

  .care__content {
    flex: 1;
  }
}

/* ============================================================
   REVIEWS
   ============================================================ */
.reviews {
  padding: var(--section-gap) 0;
  background: var(--color-bg-warm);
}

/* Star Graph */
.reviews__graph {
  display: flex;
  gap: 32px;
  align-items: center;
  background: var(--color-card);
  border: 1px solid var(--color-border);
  border-radius: 16px;
  padding: 28px;
  margin-bottom: 32px;
  flex-wrap: wrap;
}

.reviews__graph-summary {
  display: flex;
  flex-direction: column;
  align-items: center;
  min-width: 100px;
}

.reviews__graph-score {
  font-family: var(--font-display);
  font-size: clamp(4rem, 12vw, 5rem);
  /* 極端に巨大化 */
  font-weight: 300;
  color: var(--color-text);
  line-height: 1;
}

.reviews__graph-stars {
  color: var(--color-gold);
  font-size: 1rem;
  margin: 6px 0 4px;
  letter-spacing: 2px;
}

.reviews__graph-count {
  font-size: 0.7rem;
  color: var(--color-text-sub);
}

.reviews__graph-bars {
  flex: 1;
  min-width: 200px;
}

.reviews__graph-row {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 6px;
}

.reviews__graph-label {
  font-size: 0.75rem;
  color: var(--color-text-sub);
  width: 24px;
}

.reviews__graph-bar {
  flex: 1;
  height: 8px;
  background: var(--color-bg-warm);
  border-radius: 4px;
  overflow: hidden;
}

.reviews__graph-fill {
  height: 100%;
  width: 0%;
  background: var(--color-gold);
  border-radius: 4px;
  transition: width 1.2s var(--ease);
}

.reviews__graph-fill.animated {
  /* JSによる幅のアニメーション用（幅はインラインスタイルで動的に指定） */
  min-width: 1%;
}

.reviews__graph-pct {
  font-size: 0.7rem;
  color: var(--color-text-sub);
  width: 30px;
  text-align: right;
}

/* Review Cards */
.reviews__cards {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.reviews__card {
  background: var(--color-card);
  border: 1px solid var(--color-border);
  border-radius: 14px;
  padding: 24px;
}

.reviews__card-stars {
  color: var(--color-gold);
  font-size: 0.875rem;
  letter-spacing: 2px;
  margin-bottom: 10px;
}

.reviews__card-title {
  font-family: var(--font-serif);
  font-size: 1rem;
  font-weight: 500;
  margin-bottom: 10px;
}

.reviews__card-text {
  font-size: 0.875rem;
  color: var(--color-text-sub);
  line-height: 1.9;
  margin-bottom: 12px;
}

.reviews__card-meta {
  font-size: 0.75rem;
  color: var(--color-text-sub);
  margin-bottom: 10px;
}

.reviews__card-tags {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
}

.reviews__tag {
  font-size: 0.7rem;
  padding: 4px 12px;
  background: var(--color-tag);
  border-radius: 100px;
  color: var(--color-accent);
  letter-spacing: 0.02em;
}

@media (min-width: 640px) {
  .reviews__cards {
    flex-direction: row;
  }

  .reviews__card {
    flex: 1;
  }
}

/* ============================================================
   HOTELS
   ============================================================ */
.hotels {
  padding: var(--section-gap) 0;
}

.hotels__grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 20px;
}

.hotels__card {
  border-radius: 14px;
  overflow: hidden;
  background: var(--color-card);
  border: 1px solid var(--color-border);
  transition: transform 0.4s var(--ease);
}

.hotels__card:hover {
  transform: translateY(-4px);
}

.hotels__card-img {
  aspect-ratio: 16/10;
  overflow: hidden;
}

.hotels__card-img img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.hotels__desc {
  text-align: center;
  font-size: 0.875rem;
  color: var(--color-text-sub);
  margin-top: -24px;
  margin-bottom: 32px;
  line-height: 1.8;
}

.hotels__card-name {
  font-family: var(--font-serif);
  font-size: 1rem;
  font-weight: 500;
  padding: 16px 20px 4px;
}

.hotels__card-type {
  font-size: 0.75rem;
  color: var(--color-accent);
  padding: 0 20px 8px;
  letter-spacing: 0.05em;
}

.hotels__card-detail {
  font-size: 0.8125rem;
  color: var(--color-text-sub);
  padding: 0 20px 20px;
  line-height: 1.7;
}

@media (min-width: 640px) {
  .hotels__grid {
    grid-template-columns: repeat(3, 1fr);
  }
}

/* ============================================================
   CROSS SELL - おすすめセット
   ============================================================ */
.cross-sell {
  padding: var(--section-gap) 0;
  background: var(--color-bg);
}

.cross-sell__grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 24px;
  margin-top: 40px;
}

.cross-sell__card {
  display: flex;
  flex-direction: column;
  background: #fff;
  border: 1px solid var(--color-border);
  border-radius: 16px;
  overflow: hidden;
  text-decoration: none;
  color: var(--color-text);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.cross-sell__card:hover {
  transform: translateY(-4px);
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.08);
}

.cross-sell__img {
  width: 100%;
  aspect-ratio: 1;
  background: var(--color-bg-warm);
}

.cross-sell__img img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.cross-sell__img-placeholder {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--color-text-sub);
}

.cross-sell__info {
  padding: 24px 20px;
  text-align: center;
}

.cross-sell__title {
  font-family: var(--font-serif);
  font-size: 1.1rem;
  font-weight: 500;
  margin-bottom: 8px;
}

.cross-sell__price {
  font-size: 0.95rem;
  color: var(--color-text-sub);
  font-weight: 500;
}

/* ============================================================
   GIFT
   ============================================================ */
.gift {
  padding: var(--section-gap) 0;
  background: var(--color-bg-cream);
  color: var(--color-text);
}

.gift__card {
  display: flex;
  flex-direction: column;
  background: rgba(194, 157, 102, 0.06);
  border: 1px solid var(--color-border);
  border-radius: 20px;
  overflow: hidden;
}

.gift__img {
  aspect-ratio: 16/10;
  overflow: hidden;
}

.gift__img img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  opacity: 0.8;
}

.gift__content {
  padding: 40px var(--side-pad);
  text-align: center;
}

.gift__title {
  font-family: var(--font-serif);
  font-size: clamp(1.5rem, 6vw, 2rem);
  font-weight: 500;
  margin-bottom: 16px;
  color: var(--color-gold);
}

.gift__desc {
  font-size: 0.9375rem;
  line-height: 1.9;
  margin-bottom: 24px;
  color: rgba(255, 255, 255, 0.9);
}

.gift__note {
  font-size: 0.75rem;
  color: rgba(255, 255, 255, 0.5);
}

@media (min-width: 768px) {
  .gift__card {
    flex-direction: row;
    align-items: center;
  }

  .gift__img {
    flex: 1;
    margin-bottom: 0;
  }

  .gift__content {
    flex: 1;
    padding: 48px;
    text-align: left;
  }
}

/* ============================================================
   TRIAL
   ============================================================ */
.trial {
  padding: var(--section-gap) 0;
  background: var(--color-bg-warm);
}

.trial__box {
  background: #fff;
  border: 2px solid var(--color-gold);
  border-radius: 20px;
  padding: 40px var(--side-pad);
  text-align: center;
  box-shadow: 0 12px 40px rgba(201, 169, 110, 0.15);
}

.trial__title {
  font-family: var(--font-serif);
  font-size: clamp(1.25rem, 5vw, 1.75rem);
  font-weight: 500;
  margin-bottom: 24px;
}

.trial__price {
  display: inline-flex;
  flex-direction: column;
  align-items: center;
  background: var(--color-bg-warm);
  padding: 16px 40px;
  border-radius: 12px;
  margin-bottom: 24px;
}

.trial__label {
  font-size: 0.8125rem;
  color: var(--color-accent);
  font-weight: 600;
  letter-spacing: 0.05em;
  margin-bottom: 4px;
}

.trial__amount {
  font-family: var(--font-display);
  font-size: 2.5rem;
  font-weight: 600;
  color: var(--color-text);
  line-height: 1;
}

.trial__amount span {
  font-size: 1rem;
  font-weight: 400;
  margin-left: 4px;
}

.trial__desc {
  font-size: 0.9375rem;
  color: var(--color-text-sub);
  line-height: 1.8;
  margin-bottom: 32px;
  max-width: 500px;
  margin-left: auto;
  margin-right: auto;
}

.trial__btn {
  display: inline-block;
  padding: 16px 40px;
  background: var(--color-accent);
  color: #fff;
  font-weight: 600;
  border-radius: 100px;
  letter-spacing: 0.05em;
  text-decoration: none;
  transition: transform 0.3s ease;
}

.trial__btn:hover {
  transform: translateY(-2px);
  background: var(--color-gold);
  box-shadow: 0 8px 20px rgba(201, 169, 110, 0.3);
}

/* ============================================================
   FAQ
   ============================================================ */
.faq {
  padding: var(--section-gap) 0;
  background: var(--color-bg-warm);
}

.faq__list {
  display: flex;
  flex-direction: column;
  gap: 0;
}

.faq__item {
  border-bottom: 1px solid var(--color-border);
}

.faq__question {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20px 0;
  cursor: pointer;
  font-size: 0.9375rem;
  font-weight: 400;
  line-height: 1.6;
  list-style: none;
  letter-spacing: 0.02em;
}

.faq__question::-webkit-details-marker {
  display: none;
}

.faq__question:hover {
  color: var(--color-accent);
}

.faq__icon {
  font-size: 1.4rem;
  font-weight: 300;
  flex-shrink: 0;
  margin-left: 12px;
  transition: transform 0.3s var(--ease);
}

.faq__item[open] .faq__icon {
  transform: rotate(45deg);
}

.faq__answer {
  padding-bottom: 20px;
}

.faq__answer p {
  font-size: 0.8125rem;
  color: var(--color-text-sub);
  line-height: 1.9;
}

.faq__answer a {
  color: var(--color-accent);
  text-decoration: underline;
  text-underline-offset: 3px;
}

/* ============================================================
   CLOSING - 全画面化
   ============================================================ */
.closing {
  position: relative;
  min-height: 100svh;
  /* 完全に画面を占有する */
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}

.closing__bg {
  position: absolute;
  inset: 0;
}

.closing__bg img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.closing__overlay {
  position: absolute;
  inset: 0;
  background: rgba(26, 28, 32, 0.65);
}

.closing__content {
  position: relative;
  z-index: 1;
  text-align: center;
  color: #fff;
  padding: 40px var(--side-pad);
  width: 100%;
}

.closing__label {
  font-family: var(--font-display);
  font-size: 0.7rem;
  letter-spacing: 0.4em;
  text-indent: 0.4em;
  /* 中央ズレ補正 */
  opacity: 0.6;
  margin-bottom: 12px;
}

.closing__title {
  font-family: var(--font-display);
  font-size: clamp(2rem, 6vw, 3rem);
  font-weight: 300;
  letter-spacing: 0.12em;
  text-indent: 0.12em;
  /* 中央ズレ補正 */
  margin-bottom: 24px;
}

.closing__en {
  font-style: italic;
  color: var(--color-gold);
}

.closing__proof {
  display: flex;
  flex-direction: column;
  gap: 6px;
  margin-bottom: 32px;
}

.closing__proof span {
  font-size: 0.8125rem;
  color: rgba(255, 255, 255, 0.7);
  letter-spacing: 0.05em;
}

.closing__proof strong {
  color: #fff;
  font-weight: 600;
}

.closing__price {
  font-family: var(--font-display);
  font-size: 0.85rem;
  color: rgba(255, 255, 255, 0.75);
  letter-spacing: 0.05em;
  margin-bottom: 24px;
}

.closing__price span {
  font-size: 0.75rem;
  color: rgba(255, 255, 255, 0.5);
}

.closing__cta {
  display: inline-block;
  padding: 16px 48px;
  background: #fff;
  color: var(--color-text);
  font-size: 0.9375rem;
  font-weight: 600;
  letter-spacing: 0.08em;
  border-radius: 100px;
  transition: all 0.3s var(--ease);
}

.closing__cta:hover {
  background: var(--color-gold);
  color: #fff;
  transform: translateY(-2px);
  box-shadow: 0 8px 30px rgba(0, 0, 0, 0.2);
}

/* ============================================================
   FOOTER
   ============================================================ */
.footer {
  padding: 40px 0 24px;
  background: var(--color-bg-dark);
  color: var(--color-text-light);
}

.footer__inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 20px;
  flex-wrap: wrap;
  gap: 16px;
}

.footer__logo {
  font-family: var(--font-display);
  font-size: 1rem;
  letter-spacing: 0.15em;
}

.footer__links {
  display: flex;
  gap: 20px;
}

.footer__links a {
  font-size: 0.75rem;
  color: rgba(255, 255, 255, 0.5);
  transition: color 0.3s ease;
}

.footer__links a:hover {
  color: #fff;
}

.footer__disclaimer {
  font-size: 0.65rem;
  color: rgba(255, 255, 255, 0.25);
  margin-bottom: 8px;
}

.footer__copy {
  font-size: 0.65rem;
  color: rgba(255, 255, 255, 0.2);
}

/* ============================================================
   STICKY CTA
   ============================================================ */
.sticky-cta {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  z-index: 998;
  padding: 12px var(--side-pad) calc(12px + env(safe-area-inset-bottom, 0px));
  background: rgba(250, 249, 246, 0.95);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border-top: 1px solid var(--color-border);
  transform: translateY(100%);
  transition: transform 0.5s var(--ease);
}

.sticky-cta.visible {
  transform: translateY(0);
}

.sticky-cta.sticky {
  box-shadow: 0 -4px 20px rgba(0, 0, 0, 0.08);
  padding: 16px var(--side-pad) 24px;
}

.sticky-cta__button {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  max-width: var(--container-narrow);
  margin: 0 auto;
  background: var(--color-bg-dark);
  color: #fff;
  font-family: var(--font-serif);
  font-size: 1.125rem;
  font-weight: 500;
  letter-spacing: 0.1em;
  padding: 20px 0;
  border-radius: 100px;
  transition: all 0.3s ease;
}

.sticky-cta__button:hover {
  background: var(--color-accent);
  transform: translateY(-2px);
}

/* ============================================================
   PC Responsive (min-width: 1024px)
   ============================================================ */
@media (min-width: 1024px) {
  .hero__img {
    max-height: 65vh;
  }

  .hero__content {
    max-width: 650px;
    margin: 0 auto;
  }

  .empathy__list {
    flex-direction: row;
    max-width: 600px;
  }

  .empathy__item {
    flex: 1;
    flex-direction: column;
    text-align: center;
  }

  .reviews__cards {
    flex-direction: row;
  }

  .sticky-cta__button {
    max-width: 360px;
  }
}

/* ============================================================
   MID CTA - 中間購買誘導
   ============================================================ */
.mid-cta {
  padding: clamp(48px, 8vw, 80px) 0;
  background: var(--color-bg-warm);
}

.mid-cta__box {
  text-align: center;
}

.mid-cta__heading {
  font-family: var(--font-serif);
  font-size: clamp(1.25rem, 4vw, 1.75rem);
  font-weight: 500;
  color: var(--color-text);
  margin-bottom: 28px;
  letter-spacing: 0.04em;
  line-height: 1.6;
}

.mid-cta__btn {
  display: inline-block;
  padding: 18px 56px;
  font-family: var(--font-serif);
  font-size: 1.05rem;
  font-weight: 500;
  letter-spacing: 0.08em;
  color: #fff;
  background: linear-gradient(135deg, var(--color-gold), #c9a56c);
  border-radius: 100px;
  text-decoration: none;
  box-shadow: 0 4px 24px rgba(183, 155, 105, 0.35);
  transition: all 0.3s ease;
  position: relative;
}

.mid-cta__btn:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 32px rgba(183, 155, 105, 0.45);
}

/* パルスアニメーションで注目を集める */
.mid-cta__btn::after {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  border-radius: 100px;
  box-shadow: 0 0 0 0 rgba(194, 157, 102, 0.5);
  animation: cta-pulse 2.5s infinite;
}

@keyframes cta-pulse {
  0% { box-shadow: 0 0 0 0 rgba(194, 157, 102, 0.4); }
  70% { box-shadow: 0 0 0 12px rgba(194, 157, 102, 0); }
  100% { box-shadow: 0 0 0 0 rgba(194, 157, 102, 0); }
}

.mid-cta__micro {
  display: flex;
  justify-content: center;
  gap: 16px;
  flex-wrap: wrap;
  margin-top: 16px;
}

.mid-cta__micro-item {
  font-size: 0.8rem;
  color: var(--color-text-sub);
  letter-spacing: 0.03em;
}

/* ============================================================
   MAKUAKE 実績
   ============================================================ */
.makuake {
  padding: var(--section-gap) 0;
  background: var(--color-bg-dark);
  color: var(--color-text-light);
}

.makuake__box {
  text-align: center;
}

.makuake__header {
  margin-bottom: 40px;
}

.makuake__badge {
  display: inline-block;
  padding: 6px 20px;
  font-family: var(--font-display);
  font-size: 0.8rem;
  font-weight: 500;
  letter-spacing: 0.15em;
  color: var(--color-gold);
  border: 1px solid var(--color-gold);
  border-radius: 100px;
  margin-bottom: 20px;
}

.makuake__title {
  font-family: var(--font-serif);
  font-size: clamp(1.5rem, 5vw, 2.25rem);
  font-weight: 500;
  line-height: 1.5;
  letter-spacing: 0.04em;
}

.makuake__stats {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 0;
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: 20px;
  padding: 32px 16px;
  margin-bottom: 40px;
}

.makuake__stat {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6px;
  padding: 0 8px;
}

.makuake__stat-number {
  font-family: var(--font-display);
  font-size: clamp(1.5rem, 5vw, 2rem);
  font-weight: 500;
  color: var(--color-gold);
  letter-spacing: 0.02em;
}

.makuake__stat-label {
  font-size: 0.75rem;
  color: rgba(255, 255, 255, 0.6);
  letter-spacing: 0.05em;
}

.makuake__stat-divider {
  width: 1px;
  height: 48px;
  background: rgba(255, 255, 255, 0.15);
  flex-shrink: 0;
}

.makuake__soldout {
  margin-bottom: 32px;
}

.makuake__soldout-title {
  font-family: var(--font-serif);
  font-size: 1.1rem;
  font-weight: 500;
  color: var(--color-gold);
  margin-bottom: 20px;
  letter-spacing: 0.05em;
}

.makuake__soldout-list {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 8px;
}

.makuake__soldout-item {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px 16px;
  background: rgba(255, 255, 255, 0.04);
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: 8px;
}

.makuake__soldout-date {
  font-size: 0.8rem;
  color: rgba(255, 255, 255, 0.7);
}

.makuake__soldout-tag {
  font-size: 0.7rem;
  font-weight: 600;
  color: #e74c3c;
  background: rgba(231, 76, 60, 0.15);
  padding: 2px 10px;
  border-radius: 100px;
  letter-spacing: 0.05em;
}

.makuake__link {
  display: inline-block;
  font-size: 0.85rem;
  color: var(--color-gold);
  letter-spacing: 0.03em;
  border-bottom: 1px solid rgba(194, 157, 102, 0.4);
  padding-bottom: 2px;
  transition: border-color 0.3s ease;
}

.makuake__link:hover {
  border-color: var(--color-gold);
}

/* ============================================================
   UGC - Instagram Reels
   ============================================================ */
.ugc {
  padding: var(--section-gap) 0;
  background: var(--color-bg);
}

.ugc__sub {
  text-align: center;
  font-size: 0.95rem;
  color: var(--color-text-sub);
  margin-top: -32px;
  margin-bottom: 48px;
  letter-spacing: 0.03em;
}

.ugc__carousel {
  overflow-x: auto;
  overflow-y: hidden;
  -webkit-overflow-scrolling: touch;
  scrollbar-width: none;
  margin: 0 calc(-1 * var(--side-pad));
  padding: 0 var(--side-pad);
}

.ugc__carousel::-webkit-scrollbar {
  display: none;
}

.ugc__track {
  display: flex;
  gap: 16px;
  padding-bottom: 8px;
  scroll-snap-type: x mandatory;
}

.ugc__item {
  flex: 0 0 300px;
  scroll-snap-align: center;
  border-radius: 16px;
  overflow: hidden;
  box-shadow: 0 2px 16px rgba(0, 0, 0, 0.06);
  background: #fff;
}

.ugc__iframe {
  width: 300px;
  height: 535px;
  border: none;
}

.ugc__follow {
  text-align: center;
  margin-top: 32px;
}

.ugc__follow-link {
  display: inline-block;
  font-size: 0.9rem;
  font-weight: 500;
  color: var(--color-accent);
  letter-spacing: 0.03em;
  border-bottom: 1px solid var(--color-accent-light);
  padding-bottom: 2px;
  transition: border-color 0.3s ease;
}

.ugc__follow-link:hover {
  border-color: var(--color-accent);
}

@media (min-width: 768px) {
  .ugc__item {
    flex: 0 0 340px;
  }

  .ugc__iframe {
    width: 340px;
    height: 600px;
  }
}

/* ============================================================
   COMPARISON - 検証データ比較
   ============================================================ */
.comparison {
  padding: var(--section-gap) 0;
  background: var(--color-bg);
}

.comparison__label {
  text-align: center;
  font-family: var(--font-display);
  font-size: 0.85rem;
  letter-spacing: 0.2em;
  color: var(--color-accent);
  margin-bottom: 12px;
  text-transform: uppercase;
}

.comparison__highlights {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0;
  max-width: 640px;
  margin: 0 auto 48px;
}

.comparison__card {
  flex: 1;
  width: 100%;
  padding: 28px 24px;
  border-radius: 16px;
  text-align: center;
}

.comparison__card--musubi {
  background: linear-gradient(135deg, rgba(194, 157, 102, 0.08), rgba(194, 157, 102, 0.15));
  border: 2px solid var(--color-accent);
}

.comparison__card--other {
  background: var(--color-bg-warm);
  border: 1px solid var(--color-border);
}

.comparison__card-badge {
  display: inline-block;
  font-size: 0.75rem;
  font-weight: 700;
  letter-spacing: 0.08em;
  padding: 4px 14px;
  border-radius: 100px;
  margin-bottom: 12px;
}

.comparison__card--musubi .comparison__card-badge {
  background: var(--color-accent);
  color: #fff;
}

.comparison__card--other .comparison__card-badge {
  background: var(--color-border);
  color: var(--color-text-sub);
}

.comparison__card-value {
  font-family: var(--font-display);
  font-size: clamp(2rem, 6vw, 2.8rem);
  font-weight: 700;
  margin-bottom: 4px;
}

.comparison__card--musubi .comparison__card-value {
  color: var(--color-accent);
}

.comparison__card--other .comparison__card-value {
  color: var(--color-text-sub);
}

.comparison__card-desc {
  font-size: 0.9rem;
  color: var(--color-text-sub);
  margin-bottom: 12px;
}

.comparison__card-verdict {
  font-size: 0.85rem;
  font-weight: 700;
  letter-spacing: 0.03em;
}

.comparison__card-verdict--good {
  color: #4a8c5c;
}

.comparison__card-verdict--bad {
  color: #b5705a;
}

.comparison__vs {
  font-family: var(--font-display);
  font-size: 1.1rem;
  font-weight: 700;
  color: var(--color-text-sub);
  padding: 8px 0;
  letter-spacing: 0.15em;
}

/* グラフ画像 */
.comparison__chart {
  max-width: 720px;
  margin: 0 auto 48px;
  background: #fff;
  border-radius: 16px;
  padding: 24px 20px;
  border: 1px solid var(--color-border);
}

.comparison__chart-title {
  font-family: var(--font-serif);
  font-size: 1rem;
  font-weight: 600;
  text-align: center;
  color: var(--color-text);
  margin-bottom: 16px;
}

.comparison__chart img {
  width: 100%;
  border-radius: 8px;
}

.comparison__chart-note {
  font-size: 0.75rem;
  color: var(--color-text-sub);
  text-align: center;
  margin-top: 12px;
}

.comparison__chart-placeholder {
  padding: 60px 20px;
  text-align: center;
  background: var(--color-bg-warm);
  border-radius: 8px;
  color: var(--color-text-sub);
  font-size: 0.9rem;
}

/* 結論 */
.comparison__conclusion {
  display: flex;
  gap: 16px;
  max-width: 640px;
  margin: 0 auto;
  padding: 28px 24px;
  background: linear-gradient(135deg, rgba(194, 157, 102, 0.06), rgba(194, 157, 102, 0.12));
  border-radius: 16px;
  border-left: 4px solid var(--color-accent);
}

.comparison__conclusion-icon {
  font-size: 1.5rem;
  flex-shrink: 0;
}

.comparison__conclusion-label {
  font-size: 0.85rem;
  color: var(--color-text-sub);
  margin-bottom: 6px;
}

.comparison__conclusion-label strong {
  color: var(--color-accent);
  font-size: 1.1em;
}

.comparison__conclusion-text {
  font-size: 1rem;
  line-height: 1.7;
  color: var(--color-text);
}

.comparison__conclusion-text strong {
  color: var(--color-accent);
  font-weight: 700;
}

@media (min-width: 768px) {
  .comparison__highlights {
    flex-direction: row;
    gap: 0;
    max-width: 800px;
  }

  .comparison__vs {
    flex-shrink: 0;
    align-self: center;
    padding: 0 16px;
  }
}
</style>
